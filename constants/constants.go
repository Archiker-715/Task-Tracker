package constants

const (
	TasksFileName = "tasks.json"

	// task statuses
	Todo       = "todo"
	InProgress = "in-progress"
	Done       = "done"

	// key for marking tasks
	MarkInProgress = "mark-in-progress"
	MarkDone       = "mark-done"
)
